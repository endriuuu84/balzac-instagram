// Simplified wrapper for analyze-real
// Since we can't use ES modules, we'll use the simulated version for now

const analyzeHandler = require('./analyze-wrapper.js');

module.exports = analyzeHandler;